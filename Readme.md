Full Stack App Powered By Flask

On SqlServer Database
Deployed On Apache Server

Some Of The Orders Used For Deployment Although Not Well-Organized :

```
 2  sudo ./vmware-install.pl
    3  sudo apt-get update
    4  sudo apt-get install d-conf editor
    5  sudo apt-get install d-conf-editor editor
    6  sudo apt-get install dconf-editor editor
    7  sudo apt-get install dconf-editor
    8  sudo wget -qO- https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
    9  sudo add-apt-repository "$(wget -qO- https://packages.microsoft.com/config/ubuntu/18.04/mssql-server-2019.list)"
   10  sudo apt update
   11  sudo apt install mssql-server
   12  sudo /opt/mssql/bin/mssql-conf setup
   13  curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
   14  sudo apt install curl
   15  curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
   16  curl https://packages.microsoft.com/config/ubuntu/19.10/prod.list > /etc/apt/sources.list.d/mssql-release.list
   17  sudo curl https://packages.microsoft.com/config/ubuntu/19.10/prod.list > /etc/apt/sources.list.d/mssql-release.list
   18  sudo -i
   19  sudo apt update 
   20  sudo ACCEPT_EULA=Y apt install mssql-tools unixodbc-dev
   21  echo 'export PATH="$PATH:/opt/mssql-tools/bin"' >> ~/.bash_profile
   22  echo 'export PATH="$PATH:/opt/mssql-tools/bin"' >> ~/.bashrc
   23  source ~/.bashrc
   24  sqlcmd -S localhost -U SA
   25  pip --version
   26  pip3 --version
   27  sudo apt install python3-pip
   28  pip install flask
   29  pip3 install flask
   30  pip3 install flask_bootstrap
   31  pip3 install flask_wtf wtforms wtforms.validators flask_sqlalchemy flask_login sqlalchemy
   32  sudo apt install samba
   33  sudo smbpasswd -a administrator
   34  sqlcmd -S localhost -U SA P@ssw0rd
   35  sqlcmd -S localhost -U SA -P 'P@ssw0rd'
   36  sudo chmod 777 x-special/nautilus-clipboard
   37  sudo chmod 777 IT_DB.mdf
   38  sudo chmod 777 IT_DB.ldf
   39  sudo chmod 777 IT_DB_log.ldf
   40  sudo snap install --classic code
   41  sudo apt install apache2
   42  sudo apt-get install libapache2-mod-wsgi python-dev
   43  sudo chmod 777 \vaw\www\.
   44  sudo chmod 777 \var\www\.
   45  sudo chmod 777 /var/www/
   46  sudo pip install virtualenv 
   47  sudo pip3 install virtualenv 
   48  sudo nano /etc/apache2/sites-available/FlaskApp
   49  sudo nano /etc/apache2/sites-available/FlaskApp.conf
   50  sudo a2ensite FlaskApp
   51  sudo a2dissite 000-default
   52  sudo a2ensite FlaskApp.conf
   53  sudo a2dissite 000-default
   54  udo service apache2 restart 
   55  sudo service apache2 restart 
   56  history
   57  cd flaskapp
   58  cd FlaskApp
   59  source venv/bin/activate 
   60  sudo pip3 install flask flask_bootstrap flask_wtf wtforms wtforms.validators flask_sqlalchemy flask_login sqlalchemy
   61  sudo service apache2 restart 
   62  sudo pip3 install flask flask_bootstrap flask_wtf wtforms wtforms.validators flask_sqlalchemy flask_login sqlalchemy
   63  exit
   64  python __init__.py
   65  python3 __init__.py
   66  pip3 install pyodbc
   67  python3 __init__.py
   68  pip3 install businesstimedelta
   69  python3 __init__.py
   70  sudo service apache2 restart 
   71  pip3 install flask flask_bootstrap flask_wtf wtforms wtforms.validators flask_sqlalchemy flask_login sqlalchemy
   72  sudo virtualenv venv
   73  source venv/bin/activate 
   74  pip3 install flask flask_bootstrap flask_wtf wtforms wtforms.validators flask_sqlalchemy flask_login sqlalchemy
   75  sudo pip3 install flask flask_bootstrap flask_wtf wtforms wtforms.validators flask_sqlalchemy flask_login sqlalchemy
   76  sudo service apache2 restart 
   77  source venv/bin/activate 
   78  sudo apt-get install python3-flask
   79  sudo service apache2 restart 
   80  sudo service apache2 atop
   81  sudo service apache2 stop
   82  sudo service apache2 restart 
   83  pip install Flask
   84  sudo pip install Flask
   85  sudo apt install python-pip
   86  deactivate
   87  $ sudo apt-get install libapache2-mod-wsgi-py3
   88  sudo apt-get install libapache2-mod-wsgi-py3
   89  sudo add-apt-repository universe
   90  sudo apt install python2
   91  curl https://bootstrap.pypa.io/get-pip.py --output get-pip.py
   92  sudo python2 get-pip.py
   93  source venv/bin/activate 
   94  sudo add-apt-repository universe
   95  curl https://bootstrap.pypa.io/get-pip.py --output get-pip.py
   96  sudo apt install python2
   97  sudo apt install python-pip
   98  sudo python2 get-pip.py
   99  sudo pip install flask
  100  sudo pip install flask flask_bootstrap flask_wtf wtforms wtforms.validators flask_sqlalchemy flask_login sqlalchemy
  101  sudo pip install flask flask_bootstrap flask_wtf wtforms  flask_sqlalchemy flask_login sqlalchemy
  102  deactivate
  103  sudo service apache2 restart 
  104  pip install flask-table
  105  sudo service apache2 restart 
  106  source venv/bin/activate 
  107  sudo pip install flask-table
  108  deactivate
  109  sudo service apache2 restart 
  110  source venv/bin/activate 
  111  sudo pip install pyodbc
  112  deactivate
  113  sudo service apache2 restart 
  114  source venv/bin/activate 
  115  sudo pip install pyodbc
  116  sudo pip3 install pyodbc
  117  sudo pip install pyodbc
  118  source venv/bin/activate 
  119  sudo pip install pyodbc
  120  pip install pyodbc
  121  sudo pip install pyodbc
  122  sudo pip3 install pyodbc
  123  sudo apt-get install -y python-pyodbc
  124  pip install --allow-external pyodbc --allow-unverified pyodbc pyodbc
  125  sudo pip install pyodbc --allow-unverified pyodbc pyodbc
  126  sudo pip install pyodbc  pyodbc pyodbc
  127  pip install https://storage.googleapis.com/google-code-archive-downloads/v2/code.google.com/pyodbc/pyodbc-3.0.7.zip
  128  sudo pip install https://storage.googleapis.com/google-code-archive-downloads/v2/code.google.com/pyodbc/pyodbc-3.0.7.zip
  129  deactivate
  130  sudo service apache2 restart 
  131  sudo apt-get update -y
  132  sudo apt-get install -y python-pyodbc
  133  sudo apt-get install unixodbc-dev
  134  source venv/bin/activate 
  135  sudo pip install python-dev-tools 
  136  pip2 install --upgrade pip
  137  sudo su
  138  super su
  139  sudo su
  140  sudo chmod 777 .
  141  sudo chmod 777 FlaskApp
  142  sudo chmod 777 .
  143  sudo chmod 777 
  144  sudo chmod 777 flaskapp.wsgi
  145  sudo chmod 777 __init__.py
  146  sudo chmod 777 .
  147  sudo chmod -R a+rwx FlaskApp
  148  cd ..
  149  sudo chmod -R a+rwx FlaskApp
  150  sudo chmod 777 error.log
  151  sudo service apache2 restart 
  152  sudo pip install businesstimedelta
  153  sudo service apache2 restart 
  154  python3 --version
  155  sudo service apache2 restart 
```
